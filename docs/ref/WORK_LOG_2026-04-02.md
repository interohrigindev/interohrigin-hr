# 작업 내역 — 2026-04-02

## 환경 정보
- **운영 DB**: Supabase PRO `ckzbzumycmgkcpyhlclb`
- **GitHub**: `interohrigindev/interohrigin-hr` → `main` 브랜치
- **배포**: Cloudflare Pages `interohrigin-hr2.pages.dev`
- **커밋 수**: 15건 (`6addec7` ~ `e7624a0`)

---

## 완료 작업

### 1. PDF 다운로드 한글 깨짐 수정
- `pdf-fonts.ts` 분리 구조 유지 (원본 복원)
- 기존 청크 변환 로직 정상 동작 확인

### 2. 채용공고 삭제 오류 해결
- **원인**: `ai_accuracy_log`, `transcriptions` 테이블의 FK에 `ON DELETE CASCADE` 누락 + DELETE RLS 정책 부재
- **해결**:
  - 운영 DB에 FK CASCADE 적용 (SQL Editor에서 실행 완료)
  - `SECURITY DEFINER` 트리거 `cascade_delete_job_posting` 생성 (RLS 우회)
  - `useRecruitment.ts`에 하위 테이블 순차 삭제 안전장치 추가
  - 10개 테이블에 DELETE RLS 정책 추가
- **마이그레이션**: `039_fix_candidate_fk_cascade.sql`, `040_add_delete_policies_recruitment.sql`

### 3. 월간 점검 UI 개선
- 프로젝트명 + 특이사항 태그(이슈/성과/칭찬/제안/기타) 테이블 형태 UI 추가
- 관리자 뷰에서도 동일 테이블 형태 표시
- DB 컬럼 추가: `monthly_checkins.project_name`, `monthly_checkins.special_notes`
- **마이그레이션**: `038_monthly_checkin_enhance_and_task_images.sql`

### 4. 작업 설명란 이미지 첨부
- 작업 생성/수정 다이얼로그에 이미지 첨부 영역 추가 (다중 업로드, 미리보기, 삭제)
- DB 컬럼 추가: `tasks.images` (JSONB)
- Storage: `chat-attachments` 버킷 사용, 5MB 제한

### 5. 사전 질의서 재발송 버튼
- `survey_sent` + 미응답 상태에서 재발송 버튼 추가
- 기존 `/api/send-email` 자동발송 로직 유지 (원본 복원)

### 6. Drive 녹화 파일 가져오기 오류 수정
- **원인**: Google Meet 녹화 파일명에 확장자 없음 → Storage key에 특수문자 포함
- **해결**: 확장자 없거나 비정상이면 기본값 `mp4` 사용
- `interview-recordings` 버킷 `public: true`로 변경 (운영 DB)

### 7. AI 분석 JSON 파싱 오류 수정
- 화상/대면 면접, 종합 분석, 1차 분석, 사전질의서 분석 — 4곳 모두 3단계 폴백 적용
  1. 직접 파싱
  2. 코드블록 제거 + trailing comma 정리
  3. 문자열 내부 줄바꿈 이스케이프
- `transcribe.ts`에 `responseMimeType: 'application/json'` 추가

### 8. AI 면접 분석 컨텍스트 강화
- 녹음/녹화 분석 시 지원자 전체 히스토리를 AI에 전달
  - 지원 직무 + 요건, 인재상, 이력서 분석, 사전질의서 응답, 이전 면접 결과
- 프롬프트에 직무 적합성/인재상 부합도 평가 지시 추가

### 9. 2차 면접 장소 고정 + 지도 링크
- 기본 주소: `서울 강남구 선릉로121길 5, 3층(논현동, 인터오리진타워)`
- 스케줄 폼: 입력란 → 고정 주소 표시 + 네이버지도/카카오맵 링크
- 대면면접 이메일: 지도 버튼 + 1층 도착 안내 (010-3062-0070)

### 10. 합격 조건 간소화
- 연봉 / 수습급여 / 직무명 / 입사예정일 4개 필드만 표시
- 정규직 전환 급여 필드 제거

### 11. 이메일 템플릿 편집기 (채용 설정)
- Tiptap 에디터 기반 WYSIWYG 편집기 구현 (이미지, 링크, 색상, 정렬 등)
- 기본 템플릿 6종 (사전질의서, 면접안내, 합격, 불합격, 조건불합격, 면접관안내)
- DB 테이블 `recruitment_email_templates` 생성 + 기본 데이터 삽입
- **현재 메뉴 비활성화** — 이메일은 코드 레벨에서 직접 수정하는 방식으로 운영

### 12. 이메일 문구 업데이트
- 회사명: `인터오리진아이앤씨` / `Interohrigin I&C` 통일
- 문의 이메일: `admin@interohriginhr.com`
- 대면면접 안내: 1층 도착 시 010-3062-0070 연락 안내 추가
- 불합격 이메일: 사용자 제공 문구로 전면 교체

### 13. 테스트 데이터 정리
- 운영 DB 지원자 29명 + 하위 데이터 전체 삭제

---

## 운영 DB 마이그레이션 (SQL Editor에서 실행 완료)
- 이메일 템플릿 테이블 + 기본 데이터 6종
- FK CASCADE (`ai_accuracy_log`, `transcriptions`)
- CASCADE 삭제 트리거 (`cascade_delete_job_posting`)
- DELETE RLS 정책 (11개 테이블)
- 월간점검/작업 컬럼 추가

---

## 알려진 이슈 / 미해결 사항
- 없음. 모든 요청 사항 반영 완료.

---

## 수정된 주요 파일
| 파일 | 변경 내용 |
|------|-----------|
| `src/lib/email-templates.ts` | 회사명, 문의주소, 불합격 문구, 대면면접 안내 |
| `src/hooks/useRecruitment.ts` | 채용공고 삭제 하위 테이블 순차 삭제 |
| `src/hooks/useMonthlyCheckin.ts` | 프로젝트/특이사항 저장 |
| `src/routes/monthly-checkin.tsx` | 프로젝트/특이사항 테이블 UI |
| `src/routes/work/tasks.tsx` | 이미지 첨부 기능 |
| `src/routes/recruitment/candidate-report.tsx` | 재발송 버튼, 합격조건, JSON 파싱 |
| `src/routes/recruitment/schedules.tsx` | 면접 장소 고정 + 지도 링크 |
| `src/routes/recruitment/settings.tsx` | 이메일 템플릿 편집기 (메뉴 비활성화) |
| `src/components/recruitment/InterviewAnalysis.tsx` | AI 컨텍스트 강화, Drive 파일명 수정 |
| `src/lib/recruitment-ai.ts` | 종합 분석 JSON 파싱 3단계 폴백 |
| `functions/api/transcribe.ts` | responseMimeType, 컨텍스트 프롬프트, JSON 파싱 |
| `src/types/employee-lifecycle.ts` | CheckinNote 타입 추가 |
| `src/types/work.ts` | TaskImage 타입 추가 |
| `src/App.tsx` | 채용 설정 라우트 추가 |
| `src/components/layout/Sidebar.tsx` | 채용 설정 메뉴 (추가 후 비활성화) |
